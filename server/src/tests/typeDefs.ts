// import "../schema"

/**
 * Now the server has to be async initialized with InitApolloServer func.
 */
// import { apolloServer } from "../config/server";
import { expect } from "chai";
/*
describe("Testing GraphQL schema using apolloServer.executeOperation()", () => {
  it("Request Query.test should return 'correct test'", async () => {
    // arrange
    const query = "{ test }";
    // act
    const result = await apolloServer.executeOperation({
      query,
    });
    // assert
    expect(result.errors).to.be.undefined;
    expect(result.data?.test).to.equal("correct test");
  });
});
*/
